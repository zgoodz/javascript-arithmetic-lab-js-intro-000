function add(a, b) {
  var number = a+= b
  return number
}

function subtract(a, b) {
  var number = b -= a
  return number
}

function multiply(a, b) {
  var number = a *= b
  return number
}

function divide(a, b) {
  var number = a/= b
  return number
}
